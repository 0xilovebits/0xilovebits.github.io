<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://0xilovebits.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://0xilovebits.github.io/" rel="alternate" type="text/html" /><updated>2025-08-06T21:09:29+00:00</updated><id>https://0xilovebits.github.io/feed.xml</id><title type="html">0xilovebits</title><subtitle>Reverse Engineering | Malware Analysis</subtitle><entry><title type="html">Perfect Pair – Android Reverse Engineering Challenge</title><link href="https://0xilovebits.github.io/reverse-perfect-pair-ascwg" rel="alternate" type="text/html" title="Perfect Pair – Android Reverse Engineering Challenge" /><published>2025-08-01T00:00:00+00:00</published><updated>2025-08-01T00:00:00+00:00</updated><id>https://0xilovebits.github.io/perfect-pair</id><content type="html" xml:base="https://0xilovebits.github.io/reverse-perfect-pair-ascwg"><![CDATA[<h2 id="-my-first-ever-writeup--and-its-a-first-blood-">🎉 My First Ever Writeup – and Its a First Blood! 🔥</h2>

<p>Hey! This is my first CTF writeup ever 😄, and guess what? I managed to grab the <strong>First Blood</strong> on this challenge 💉 — a proud moment for sure!
<br />
<br /></p>

<hr />

<h3 id="️-tools-used">🛠️ Tools Used</h3>

<table>
  <thead>
    <tr>
      <th>Tool</th>
      <th>Purpose</th>
      <th>Link</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><a href="https://hex-rays.com/ida-pro/" target="_blank">IDA Pro</a></td>
      <td>Static disassembler for binary analysis</td>
      <td>🔗 <a href="https://hex-rays.com/ida-pro/" target="_blank">hex-rays.com</a></td>
    </tr>
    <tr>
      <td><a href="https://github.com/skylot/jadx" target="_blank">JADX</a></td>
      <td>Decompile APKs to Java</td>
      <td>🔗 <a href="https://github.com/skylot/jadx" target="_blank">GitHub</a></td>
    </tr>
    <tr>
      <td> </td>
      <td> </td>
      <td> </td>
    </tr>
  </tbody>
</table>

<hr />

<p>This challenge, titled <strong>Perfect Pair</strong>, was part of the <strong><a href="https://www.ascyberwargames.com/" target="_blank">Arab Security Cyber Wargames 2025</a></strong>.<br />
It was a mobile reverse engineering challenge involving an Android APK.</p>

<h2 id="-static-analysis">🔍 Static Analysis</h2>

<p>After Downloading the APK, the first step was to open it in <a href="https://github.com/skylot/jadx" target="_blank">JADX</a> to explore its contents. I headed straight to the <code class="language-plaintext highlighter-rouge">AndroidManifest.xml</code> to identify the app entry point</p>

<p><img src="/images/2025-08-01-perfect-pair/manifest-entrypoint.png" alt="AndroidManifest Entry Point" style="display: block; margin: 0 auto; max-width: 100%; height: auto;" /></p>

<p style="text-align: center; margin-top: 8px; font-size: 14px; color: #666;">
AndroidManifest.xml showing the application entry point.
</p>

<p>While exploring the decompiled code, I noticed that the app was compiled using the <a href="https://developer.android.com/compose" target="_blank">Jetpack Compose Compiler</a> — Google modern declarative UI toolkit for building native Android apps faster and more easily.</p>

<p>This made the static analysis noticeably harder. Unlike traditional Android apps that use XML layouts and standard activity flows, Compose-based apps generate a lot of synthetic and compiler-generated classes and methods. These often result in unreadable or abstract bytecode when decompiled, which makes it tricky to trace UI logic or flow between screens using tools like JADX alone.</p>

<p>The entry point, <code class="language-plaintext highlighter-rouge">asc.wargames.perfectpair.MainActivity</code>, is where our journey begins.<br />
Inside its <code class="language-plaintext highlighter-rouge">onCreate()</code> method, the app sets its UI content using Jetpack Compose:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ComponentActivity2</span><span class="o">.</span><span class="na">setContent</span><span class="n">$default</span><span class="o">(</span>
    <span class="k">this</span><span class="o">,</span>
    <span class="kc">null</span><span class="o">,</span>
    <span class="nc">ComposableSingletons</span><span class="n">$MainActivityKt</span><span class="o">.</span><span class="na">INSTANCE</span><span class="o">.</span><span class="na">m6401getLambda3</span><span class="n">$app_debug</span><span class="o">(),</span>
    <span class="mi">1</span><span class="o">,</span>
    <span class="kc">null</span>
<span class="o">);</span>
</code></pre></div></div>
<p>This leads us down a rabbit hole of generated singleton classes. The call goes to <code class="language-plaintext highlighter-rouge">getLambda3$app_debug</code>, which in turn calls <code class="language-plaintext highlighter-rouge">getLambda2$app_debug</code>, then <code class="language-plaintext highlighter-rouge">getLambda1$app_debug</code>, and finally, this chain invokes the core navigation setup method: <code class="language-plaintext highlighter-rouge">MainActivityKt.PerfectPairNavHost()</code>. This chaining is a typical pattern for how the Compose compiler wires things up behind the scenes.</p>

<p><img src="/images/2025-08-01-perfect-pair/navhost.png" alt="Navigation Setup Method" style="display: block; margin: 0 auto; max-width: 100%; height: auto;" /></p>
<p style="text-align: center; margin-top: 8px; font-size: 14px; color: #666;">
navigation setup method
</p>

<p>This gives us a complete blueprint of the app’s structure:</p>

<ul>
  <li>welcome</li>
  <li>register</li>
  <li>login</li>
  <li>mainmenu</li>
  <li>download</li>
  <li>upload</li>
  <li>flag</li>
</ul>

<p>An interesting observation here is that while routes like <code class="language-plaintext highlighter-rouge">upload</code> and <code class="language-plaintext highlighter-rouge">flag</code> are clearly defined, they aren’t directly accessible from the main user interface flow. After logging in, the user is taken to the <code class="language-plaintext highlighter-rouge">mainmenu</code>, which only seems to offer two visible buttons: <strong>“Download”</strong> (leading to the <code class="language-plaintext highlighter-rouge">download</code> screen <strong>Download firmware_v2.bin</strong>) and <strong>“Logout”</strong> (which presumably calls the <code class="language-plaintext highlighter-rouge">logout</code> route to end the session).</p>

<p>Now that the app structure is clear and the main accessible routes are identified, it time to analyze the most intriguing part — the <code class="language-plaintext highlighter-rouge">flag</code> activity 🔍.
The <code class="language-plaintext highlighter-rouge">upload</code> route will be analyzed later, after confirming the behavior of the <code class="language-plaintext highlighter-rouge">flag</code> activity.</p>

<p><img src="/images/2025-08-01-perfect-pair/flag_activity.png" alt="Navigation Setup Method" style="display: block; margin: 0 auto; max-width: 100%; height: auto;" /></p>
<p style="text-align: center; margin-top: 8px; font-size: 14px; color: #666;">
Flag Activity
</p>

<p>Upon inspecting the <code class="language-plaintext highlighter-rouge">FlagActivity</code>, it turns out there nothing useful or interesting happening directly within it However, scrolling further through the code reveals a method near the end named <code class="language-plaintext highlighter-rouge">redirectToRealFlagActivity()</code>.</p>

<p>This method doesnt contain any validation or complex logic — it simply launches another activity called <code class="language-plaintext highlighter-rouge">RealFlagActivity</code>.</p>

<p>That where things might get more interesting.</p>

<p>So, let move on and see what inside <code class="language-plaintext highlighter-rouge">RealFlagActivity</code> 👀.</p>

<p><img src="/images/2025-08-01-perfect-pair/real_flag_activity.png" alt="RealFlagActivity UI" style="display: block; margin: 0 auto; max-width: 100%; height: auto;" /></p>
<p style="text-align: center; margin-top: 8px; font-size: 14px; color: #666;">
RealFlagActivity
</p>

<p>Right under the cato image there a method called <code class="language-plaintext highlighter-rouge">secretSignature</code> that takes two byte arrays: <code class="language-plaintext highlighter-rouge">data</code> and <code class="language-plaintext highlighter-rouge">key</code>. It sets up an HMAC using <code class="language-plaintext highlighter-rouge">HmacSHA256</code> like this:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kd">final</span> <span class="nc">String</span> <span class="nf">secretSignature</span><span class="o">(</span><span class="kt">byte</span><span class="o">[]</span> <span class="n">data</span><span class="o">,</span> <span class="kt">byte</span><span class="o">[]</span> <span class="n">key</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">NoSuchAlgorithmException</span> <span class="o">{</span>
    <span class="nc">Mac</span><span class="o">.</span><span class="na">getInstance</span><span class="o">(</span><span class="s">"HmacSHA256"</span><span class="o">);</span>
    <span class="k">new</span> <span class="nf">SecretKeySpec</span><span class="o">(</span><span class="n">key</span><span class="o">,</span> <span class="s">"HmacSHA256"</span><span class="o">);</span>
    <span class="k">return</span> <span class="s">""</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>At first glance, it seems important — maybe it was supposed to verify something or generate a secure value — but after searching through the entire codebase, it turns out the method is never actually used.</p>

<p>It might just be a leftover, or maybe a decoy. Either way, its not something we need to worry about for now.</p>

<p>Also, that cat image might be a hint for something well discover later. But from a code perspective, there nothing functional happening in either <code class="language-plaintext highlighter-rouge">FlagActivity</code> or <code class="language-plaintext highlighter-rouge">RealFlagActivity</code>.</p>

<p>So lets move on and dig into what happening inside <code class="language-plaintext highlighter-rouge">upload</code> activity.</p>

<p>After digging into the functionality of <code class="language-plaintext highlighter-rouge">upload</code> activity, I found that it allows the user to select a firmware file — similar to what happens in the download flow — and then uploads it to the server.</p>

<p><img src="/images/2025-08-01-perfect-pair/upload_firmware.png" alt="Upload Firmware Method" style="display: block; margin: 0 auto; max-width: 100%; height: auto;" /></p>
<p style="text-align: center; margin-top: 8px; font-size: 14px; color: #666;">
Upload Firmware Method
</p>

<p>While tracing the upload logic, I noticed there a <strong>Response Interceptor</strong> hooked into the network layer. This interceptor listens to the server response and — surprise — that where the flag is returned!</p>

<p><img src="/images/2025-08-01-perfect-pair/response_interceptor.png" alt="Response Interceptor Method" style="display: block; margin: 0 auto; max-width: 100%; height: auto;" /></p>
<p style="text-align: center; margin-top: 8px; font-size: 14px; color: #666;">
Response Interceptor Method
</p>

<p>So now its clear: the server is expecting a specific firmware file. If we upload the right one, it sends back the flag in the response.</p>

<p>Time to figure out what that expected firmware looks like.</p>

<p>At this point, I couldnt find anything else interesting in the code itself, so I thought — maybe there something useful hidden in the <code class="language-plaintext highlighter-rouge">resources</code>.</p>

<p>And bingo! I spotted a native library bundled inside: <code class="language-plaintext highlighter-rouge">liblibsign.so</code>.</p>

<p>And… its <em>not</em> used anywhere in the app.</p>

<p>Now <em>that</em> raised some eyebrows.</p>

<p>Looks like the developer left behind something they really shouldn’t  have. Either they forgot to strip it out… or they didnt realize how revealing it might be. Either way, were not complaining 😏</p>

<p>Lets crack it open in IDA Pro and see what secrets its hiding.</p>

<p>The first thing I always check when opening a native library is the list of <strong>exported functions</strong> — they usually give a quick overview of what the library is meant to do.</p>

<p><img src="/images/2025-08-01-perfect-pair/ida_exports.png" alt="Exports From ida Pro" style="display: block; margin: 0 auto; max-width: 100%; height: auto;" /></p>
<p style="text-align: center; margin-top: 8px; font-size: 14px; color: #666;">
Exports From ida Pro
</p>

<p>And from the look of it… there are some <strong>very interesting functions</strong> &amp; <strong>Secrets</strong> here 👀</p>

<p>Let’s analyze this and see what the developer accidentally left for us.</p>

<p>First, let’s take a quick look at the <strong>SECRET_KEY</strong> Export.</p>

<p><img src="/images/2025-08-01-perfect-pair/secret_keys.png" alt="SECRET_KEY" style="display: block; margin: 0 auto; max-width: 100%; height: auto;" /></p>

<p>Looks like there are a few keys here that might come in handy later.</p>

<p>Now let’s check out the <code class="language-plaintext highlighter-rouge">Java_asc_wargames_perfectpair_NativeLib_getKey</code> function.</p>

<p><img src="/images/2025-08-01-perfect-pair/getKeyfunc.png" alt="GetKeyFunc" style="display: block; margin: 0 auto; max-width: 100%; height: auto;" /></p>

<p>This one returns a key <code class="language-plaintext highlighter-rouge">0x42</code> (66 decimal) — maybe its used in an XOR or something similar.</p>

<p>Next up, the function that really caught my eye is <code class="language-plaintext highlighter-rouge">Java_asc_wargames_perfectpair_NativeLib_processData</code>. The name suggests it’s pretty important, so let’s dive into it.</p>

<p><img src="/images/2025-08-01-perfect-pair/processDatafunc.png" alt="processDatafunc" style="display: block; margin: 0 auto; max-width: 100%; height: auto;" /></p>

<p>From a quick look, this function reads the input byte array, XORs each byte with values derived from <code class="language-plaintext highlighter-rouge">secret_keys</code>, and returns a new byte array. The way the key is accessed and applied hints that this function might be handling decryption or data transformation. Let’s break it down.</p>

<p>What is the key calculated by <strong>(&amp;secret_keys + loop_counter % 4 - 6543)</strong>?</p>

<p>The <code class="language-plaintext highlighter-rouge">secret_keys</code> symbol points to:
.got.plt:0000202C secret_keys     dd offset stru_1F5C</p>

<p>Now let’s assume <code class="language-plaintext highlighter-rouge">loop_counter</code> is 0.</p>

<p>Then the calculation becomes:
(0x202C + 0 % 4 - 6543) = 0x69D</p>

<p>Address <code class="language-plaintext highlighter-rouge">0x69D</code> corresponds to:
.rodata:0000069D aMeow           db ‘meow’,0</p>

<p>So, it reads 4 bytes from this address — meaning the key used here is <code class="language-plaintext highlighter-rouge">"meow"</code>.</p>

<h2 id="lets-play-with-the-firmware">Lets Play With the Firmware!</h2>

<p>Now that I have collected two interesting keys:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">66</code> (from <code class="language-plaintext highlighter-rouge">getKey()</code>)</li>
  <li><code class="language-plaintext highlighter-rouge">"meow"</code> (from the secret memory location)</li>
</ul>

<p>I had a strong hunch that the function <code class="language-plaintext highlighter-rouge">processData()</code> in the native library is responsible for <strong>firmware encryption</strong>.</p>

<p>So, let’s test this out!</p>

<h3 id="xor-with-key-66">XOR with Key <code class="language-plaintext highlighter-rouge">66</code></h3>

<p>I tried to XOR-decrypt the downloaded firmware using the key <code class="language-plaintext highlighter-rouge">66</code>.</p>

<p>And guess what? It worked!
The result looked like this:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>USERNAME=_______________
TEAM_ID=0000
DATA=SMARTLOCK_FIRMWARE
SIGNATURE=________________________________
</code></pre></div></div>

<p>Now things are making more sense!</p>

<p>We can now confidently say:</p>
<ul>
  <li><strong>Firmware encryption key = <code class="language-plaintext highlighter-rouge">66</code></strong></li>
  <li>there is a  <code class="language-plaintext highlighter-rouge">SIGNATURE</code> field… and we just so happen to have another key (<code class="language-plaintext highlighter-rouge">"meow"</code>) left unused…</li>
</ul>

<p>Hmm… where have we seen <code class="language-plaintext highlighter-rouge">meow</code> before?</p>

<p>Right! Back in <code class="language-plaintext highlighter-rouge">RealFlagActivity</code>, under the cat image, there was a function called <code class="language-plaintext highlighter-rouge">secretSignature</code>. It takes <code class="language-plaintext highlighter-rouge">data</code> and <code class="language-plaintext highlighter-rouge">key</code>, and uses HMAC-SHA256.</p>

<p>Looks like we’ve just cracked the full flow!</p>

<hr />

<h2 id="final-conclusion">Final Conclusion</h2>

<p>Let’s sum it up:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">processData()</code> encrypts/decrypts firmware with XOR using key <code class="language-plaintext highlighter-rouge">66</code>.</li>
  <li><code class="language-plaintext highlighter-rouge">SIGNATURE</code> is generated via <code class="language-plaintext highlighter-rouge">HMAC-SHA256</code> using key <code class="language-plaintext highlighter-rouge">"meow"</code> (from memory address <code class="language-plaintext highlighter-rouge">0x69D</code>).</li>
  <li>The cat image + the unused <code class="language-plaintext highlighter-rouge">secretSignature()</code> were actually <strong>important hints</strong>.</li>
</ul>

<h2 id="lets-automate-this">Let’s automate this!</h2>

<p>write python script to genreate firmware</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">hmac</span>
<span class="kn">import</span> <span class="n">hashlib</span>

<span class="n">SIGN_KEY</span> <span class="o">=</span> <span class="sa">b</span><span class="sh">"</span><span class="s">meow</span><span class="sh">"</span>
<span class="n">XOR_KEY</span> <span class="o">=</span> <span class="mi">66</span>
<span class="n">USERNAME</span> <span class="o">=</span> <span class="sh">"</span><span class="s">ilovebits_ahmed</span><span class="sh">"</span>
<span class="n">TEAM_ID</span> <span class="o">=</span> <span class="sh">"</span><span class="s">0007</span><span class="sh">"</span>
<span class="n">firmware_content</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"""</span><span class="s">USERNAME=</span><span class="si">{</span><span class="n">USERNAME</span><span class="si">}</span><span class="s">
TEAM_ID=</span><span class="si">{</span><span class="n">TEAM_ID</span><span class="si">}</span><span class="s">
DATA=SMARTLOCK_FIRMWARE
</span><span class="sh">"""</span><span class="p">.</span><span class="nf">replace</span><span class="p">(</span><span class="sh">"</span><span class="se">\n</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="se">\r\n</span><span class="sh">"</span><span class="p">)</span>
<span class="n">signature</span> <span class="o">=</span> <span class="n">hmac</span><span class="p">.</span><span class="nf">new</span><span class="p">(</span><span class="n">SIGN_KEY</span><span class="p">,</span> <span class="n">firmware_content</span><span class="p">.</span><span class="nf">encode</span><span class="p">(),</span> <span class="n">hashlib</span><span class="p">.</span><span class="n">sha256</span><span class="p">).</span><span class="nf">hexdigest</span><span class="p">()</span>
<span class="n">final_firmware</span> <span class="o">=</span> <span class="n">firmware_content</span> <span class="o">+</span> <span class="sa">f</span><span class="sh">"</span><span class="s">SIGNATURE=</span><span class="si">{</span><span class="n">signature</span><span class="si">}</span><span class="se">\r\n</span><span class="sh">"</span>
<span class="n">encoded_bytes</span> <span class="o">=</span> <span class="nf">bytes</span><span class="p">([</span><span class="n">b</span> <span class="o">^</span> <span class="n">XOR_KEY</span> <span class="k">for</span> <span class="n">b</span> <span class="ow">in</span> <span class="n">final_firmware</span><span class="p">.</span><span class="nf">encode</span><span class="p">()])</span>
<span class="k">with</span> <span class="nf">open</span><span class="p">(</span><span class="sh">"</span><span class="s">patched_firmware.bin</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">wb</span><span class="sh">"</span><span class="p">)</span> <span class="k">as</span> <span class="n">f</span><span class="p">:</span>
    <span class="n">f</span><span class="p">.</span><span class="nf">write</span><span class="p">(</span><span class="n">encoded_bytes</span><span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">[+] Firmware patched and saved.</span><span class="sh">"</span><span class="p">)</span>


</code></pre></div></div>

<p>and the script to  upload the patched firmware</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">requests</span>
<span class="kn">import</span> <span class="n">os</span>

<span class="n">BASE_URL</span> <span class="o">=</span> <span class="sh">"</span><span class="s">http://34.136.125.67:5000</span><span class="sh">"</span>


<span class="k">def</span> <span class="nf">upload_firmware</span><span class="p">(</span><span class="n">jwt_token</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">file_path</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
    <span class="k">if</span> <span class="ow">not</span> <span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">exists</span><span class="p">(</span><span class="n">file_path</span><span class="p">):</span>
        <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">error</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">File not found at </span><span class="si">{</span><span class="n">file_path</span><span class="si">}</span><span class="sh">"</span><span class="p">}</span>
    <span class="n">url</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">BASE_URL</span><span class="si">}</span><span class="s">/api/firmware</span><span class="sh">"</span>
    <span class="n">headers</span> <span class="o">=</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">Authorization</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Bearer </span><span class="si">{</span><span class="n">jwt_token</span><span class="si">}</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">Content-Type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">application/octet-stream</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">}</span>

    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Uploading firmware to: </span><span class="si">{</span><span class="n">url</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">File size: </span><span class="si">{</span><span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">getsize</span><span class="p">(</span><span class="n">file_path</span><span class="p">)</span><span class="si">}</span><span class="s"> bytes</span><span class="sh">"</span><span class="p">)</span>

    <span class="k">try</span><span class="p">:</span>
        <span class="k">with</span> <span class="nf">open</span><span class="p">(</span><span class="n">file_path</span><span class="p">,</span> <span class="sh">"</span><span class="s">rb</span><span class="sh">"</span><span class="p">)</span> <span class="k">as</span> <span class="n">f</span><span class="p">:</span>
            <span class="n">file_data</span> <span class="o">=</span> <span class="n">f</span><span class="p">.</span><span class="nf">read</span><span class="p">()</span>
            <span class="n">response</span> <span class="o">=</span> <span class="n">requests</span><span class="p">.</span><span class="nf">post</span><span class="p">(</span><span class="n">url</span><span class="p">,</span> <span class="n">headers</span><span class="o">=</span><span class="n">headers</span><span class="p">,</span> <span class="n">data</span><span class="o">=</span><span class="n">file_data</span><span class="p">,</span> <span class="n">timeout</span><span class="o">=</span><span class="mi">30</span><span class="p">)</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Upload successful (Code: </span><span class="si">{</span><span class="n">response</span><span class="p">.</span><span class="n">status_code</span><span class="si">}</span><span class="s">)</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">response</span><span class="p">.</span><span class="nf">json</span><span class="p">()</span>
    <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">❌ An unexpected error occurred: </span><span class="si">{</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">error</span><span class="sh">"</span><span class="p">:</span> <span class="nf">str</span><span class="p">(</span><span class="n">e</span><span class="p">)}</span>


<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">jwt</span> <span class="o">=</span> <span class="sh">"</span><span class="s">account_jwt</span><span class="sh">"</span>
    <span class="n">patched_firmware</span> <span class="o">=</span> <span class="sh">"</span><span class="s">patched_firmware.bin</span><span class="sh">"</span>
    <span class="n">result</span> <span class="o">=</span> <span class="nf">upload_firmware</span><span class="p">(</span><span class="n">jwt_token</span><span class="o">=</span><span class="n">jwt</span><span class="p">,</span> <span class="n">file_path</span><span class="o">=</span><span class="n">patched_firmware</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="se">\n</span><span class="s">--- Server Response ---</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">result</span><span class="p">:</span>
        <span class="nf">print</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>

</code></pre></div></div>

<p>let’s run this</p>

<p><img src="/images/2025-08-01-perfect-pair/test.png" alt="Test" style="display: block; margin: 0 auto; max-width: 100%; height: auto;" /></p>

<p>And with that, the server responded with the flag! A super fun challenge that required connecting dots from the Java layer, native code, and even subtle hints in the UI.</p>]]></content><author><name></name></author><category term="ctf" /><category term="reverse" /><category term="reverse_engineering" /><category term="ASCWG" /><category term="mobile_reverse_engineering" /><category term="android" /><summary type="html"><![CDATA[🎉 My First Ever Writeup – and Its a First Blood! 🔥]]></summary></entry><entry><title type="html">Welcome to My Blog</title><link href="https://0xilovebits.github.io/welcome/" rel="alternate" type="text/html" title="Welcome to My Blog" /><published>2025-07-22T00:00:00+00:00</published><updated>2025-07-22T00:00:00+00:00</updated><id>https://0xilovebits.github.io/welcome</id><content type="html" xml:base="https://0xilovebits.github.io/welcome/"><![CDATA[<h1 id="welcome-hackers-and-learners">Welcome, Hackers and Learners!</h1>

<p>Hello and welcome to my blog!<br />
This space is where I’ll share my journey into <strong>Reverse Engineering, Malware Analysis, and Low-Level Programming</strong>.</p>

<hr />

<h2 id="about-me">About Me</h2>
<p>$ <strong>whoami</strong> → <code class="language-plaintext highlighter-rouge">0xilovebits</code><br />
I’m a self-taught reverse engineer from Egypt who loves <strong>low-level puzzles</strong>, exploring how systems work under the hood, and sharing everything I learn.<br />
🧠 <em>Always learning. Always reversing. Always sharing.</em></p>

<hr />

<h2 id="what-to-expect">What to Expect</h2>
<p>I’ll be posting about:</p>
<ul>
  <li><strong>Reverse Engineering</strong> tips and writeups.</li>
  <li><strong>Malware Analysis</strong> case studies.</li>
  <li><strong>Low-level coding</strong> (C, Assembly, (Zig &amp; Pascal)(Soon) ).</li>
  <li><strong>Technical projects</strong> like emulators and tools.</li>
</ul>

<h2 id="just-as-soon-as-i-stop-reverse-engineering-my-own-life-">…just as soon as I stop reverse engineering my own life 🤯</h2>

<hr />

<h2 id="projects--experiments">Projects &amp; Experiments</h2>
<ul>
  <li><a href="https://github.com/0xilovebits/x86-emulators-in-c"><strong>x86-emulators-in-c</strong></a>: Learning CPU internals by building emulators in C.</li>
</ul>

<hr />

<h2 id="links--contact">Links &amp; Contact</h2>
<ul>
  <li><a href="https://github.com/0xilovebits"><strong>GitHub</strong></a></li>
  <li><strong>Discord</strong> : ilovebits</li>
</ul>

<hr />

<h2 id="closing-thoughts">Closing Thoughts</h2>
<p>This blog is my personal lab notebook. Expect <strong>writeups</strong>, <strong>deep dives</strong>, and <strong>behind-the-scenes</strong> explorations of software internals.</p>

<p>Stay tuned—more content is coming soon!</p>]]></content><author><name></name></author><category term="reverse" /><category term="welcome" /><category term="welcome" /><category term="introduction" /><category term="reverse-engineering" /><summary type="html"><![CDATA[Welcome, Hackers and Learners!]]></summary></entry></feed>